package com.example.studentcourse.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // The JSP prefix/suffix is configured in application.properties.
}
